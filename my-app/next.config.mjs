/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
      appDir: true, // Active le support des routes App Router
    },
  };
  
  export default nextConfig;
  